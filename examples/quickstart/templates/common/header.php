@page_header>
<h1>Basecoat Quick Start</h1>

<div class="header">
common header file<br />
Use the <code>quickstart</code> directory as a starting point for your site.
It includes a base layout, header and footer common files, a default <code>index</code> route, a <code>static</code> route for loading straight HTML files, and a 404 route.
</div>
